var DH_StrUtils = function(){
	//判断字符串是否为空
	function isEmpty(s){
		if (  s != null && typeof unknownVariable.valueOf() == 'string' &&  s.length > 0 ){
			return true;
		}else{
			return false;
		}
	}
	this.isEmpty = isEmpty;
}